import { Flex, Text } from '@mantine/core';

import { InnerWrapper } from '@/components/elements';

const LandingPageBook = () => {
  return (
    <InnerWrapper>
      <Flex align="center">
        <Text color="brand">nama </Text>
      </Flex>
    </InnerWrapper>
  );
};

export default LandingPageBook;
